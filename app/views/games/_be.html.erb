<!-- be -->
<div class="row">
  <div class="col-sm-4">
    <button type="button" name="button"><%= link_to("#{@game.player_one_be}", games_player_one_be_path(id: @game.id), method: :post) %></button>
  </div>
  <div class="col-sm-4">
      <h1>BE</h1>
  </div>
  <div class="col-sm-4">
    <button type="button" name="button"><%= link_to("#{@game.player_two_be}", games_player_two_be_path(id: @game.id), method: :post) %></button>
  </div>
</div>
