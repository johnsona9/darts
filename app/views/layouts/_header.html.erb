<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav>
      <a class="navbar-brand" href="/">Darts</a>
      <ul class="nav navbar-nav navbar-left">
        <% if logged_in_one? %>
          <li><%= link_to @current_user_one.name, @current_user_one %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Settings", '#' %></li>
              <li class="divider"></li>
              <li>
                <li><%= link_to 'P1 Log Out', log_out_one_path, method: 'delete' %></li>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'P1 Log In', log_in_one_path %></li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if logged_in_two? %>
        <li><%= link_to @current_user_two.name, @current_user_two %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Settings", '#' %></li>
              <li class="divider"></li>
              <li>
                <%= link_to 'P2 Log Out', log_out_two_path, method: 'delete' %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'P2 Sign In', log_in_two_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
