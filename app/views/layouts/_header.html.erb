<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav>
      <a class="navbar-brand" href="/">Darts</a>
      <ul class="nav navbar-nav navbar-left">
        <% if logged_in_one? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= @current_user_one.name %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Account", @current_user_one %></li>
              <li class="divider"></li>
              <li>
                <li><%= link_to 'Log Out', log_out_path(id: 1), method: 'delete' %></li>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'P1 Log In', log_in_path %></li>
        <% end %>

        <% if logged_in_two? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= @current_user_two.name %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Account", @current_user_two %></li>
              <li class="divider"></li>
              <li>
                <%= link_to 'P2 Log Out', log_out_path(id: 2), method: 'delete' %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'P2 Log In', log_in_path %></li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if logged_in_three? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= @current_user_three.name %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Account", @current_user_three %></li>
              <li class="divider"></li>
              <li>
                <%= link_to 'P3 Log Out', log_out_path(id: 3), method: 'delete', session_num: 3 %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'P3 Log In', log_in_path %></li>
        <% end %>

        <% if logged_in_four? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= @current_user_four.name %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Account", @current_user_four %></li>
              <li class="divider"></li>
              <li>
                <%= link_to 'P4 Log Out', log_out_path(id: 4), method: 'delete' %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'P4 Log In', log_in_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
